[telegram]
bot_token = YOUR_BOT_TOKEN_HERE
allowed_user_id = YOUR_TELEGRAM_USER_ID

[storage]
# Optional. The default is data/hour_booster.sqlite3
# database_path = data/hour_booster.sqlite3

# Steam accounts are added, edited, and deleted through the private Telegram menu.
# Existing legacy [account*] sections are imported into SQLite once on first launch.
